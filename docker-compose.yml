version: "3.8"

services:
  oas-generator:
    image: node:20-alpine3.16
    working_dir: /home/node/oas-generator
    command: sh -c "npm install --from-lock-file && npm run start"
    volumes:
      - ./:/home/node/oas-generator
